services:
  - type: web
    name: valo-yt
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend main:app --bind 0.0.0.0:$PORT --timeout 120
